<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="neu.edu.madcourse.numad21su_alannapasco.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" >
        <action
            android:id="@+id/about_action"
            app:destination="@id/aboutActivity" />
        <action
            android:id="@+id/clicky_action"
            app:destination="@id/clickyActivity" />
    </activity>
    <activity
        android:id="@+id/aboutActivity"
        android:name="neu.edu.madcourse.numad21su_alannapasco.AboutActivity"
        android:label="activity_about"
        tools:layout="@layout/activity_about" />
    <activity
        android:id="@+id/clickyActivity"
        android:name="neu.edu.madcourse.numad21su_alannapasco.ClickyActivity"
        android:label="activity_clicky"
        tools:layout="@layout/activity_clicky" />
</navigation>